// import express from 'express';
// import { default as loginController } from '../app/controllers/LoginController.js';
// const loginRouter = express.Router();

// loginRouter.use('/', loginController.get);
// loginRouter.use('/:slug', loginController.show);
// loginRouter.use('/', loginController.index);

// export { loginRouter };
